{
  "engines": {
    "node": ">=20.19.0 <21 || >=22.12.0"
  },
  "scripts": {
    "build": "npm --prefix portal ci && npm --prefix portal run build",
    "start": "npm --prefix portal start",
    "portal:build": "npm --prefix portal run build",
    "portal:start": "npm --prefix portal start"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5"
  }
}
